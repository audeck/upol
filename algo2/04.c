#include <stdio.h>
#include <stdlib.h>

typedef struct node {
    int data;
    node* left;
    node* right;
} node;

typedef struct tree {
    node* root;
} tree;

void add(tree *t, int data) {

}

void print_in_order(tree t) {

}

int depth(tree t) {
    
}